name: Build my application

on: push

jobs:
  build:
    name: Build app
    runs-on: ubuntu-latest 
    steps:
    - name: Pull repo
      uses: actions/checkout@v4.1.7
    - name: List files in dir
      run: ls
